MONGODB_URI=mongodb+srv://<username>:<password>@m0.mpttgqu.mongodb.net/event_ticket_db

# NextAuth (Auth.js v5) — generate a secret with: openssl rand -base64 32
AUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000
